project.name = partyliterature
project.default_charset = utf-8
server.index = 8383
server.search = 8384

[n_id]
type=id

[v_global_guid]
index = self
tokenizer = full

[v_title]
type = title
cutlen = 102

[v_resource_content]
index =self
cutlen = 120

[v_author]
index = self
tokenizer = full

[v_parent_guid]
index =both
tokenizer = full

[v_cat_name]
index =both
tokenizer = full

[v_publish_year]
index =both

[d_publication_time]
index = both

[n_browser]
type = numeric

[v_keywords]
index =both
tokenizer = full

[v_download_src]

[bookId]

[v_parent_global]